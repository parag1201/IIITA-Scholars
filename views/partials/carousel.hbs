<carousel>
    <section class="carousel">
        <div class="swiper-container" data-aos="zoom-out-left">
            <div class="swiper-wrapper">
                {{#each profData as |prof|}}
                    <div class="swiper-slide">
                        <div class="prof-card">
                            <img src={{prof.avatar_url}} alt="">
                            <br />
                            <span>{{prof.name}}</span>
                            <p>Department of {{prof.department}}</p>
                            <div class="card-content">
                                <i class="far fa-newspaper"></i> {{prof.publications}} Publications
                                <br />
                                <i class="fas fa-link"></i> {{prof.citations}} Citations
                                <br />
                                <button type="button" class="btn btn-outline-info">View Profile</button>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
            <!-- Add Pagination -->
            <div class="swiper-pagination"></div>
        </div>
    </section>
</carousel>



